# Project Rules for Claude

## Mission

このプロジェクトの目的は、Claude Codeを人がわかりやすいように表示し、意図したタイミングでHookやSkillが起動しているかを確認することです。
Claudeは短期的に動くコードではなく、長期的に保守できる実装を優先してください。

## Core Principles

- まず既存コードを理解してから変更する
- 勝手な前提で進めない
- 不明点、矛盾、仕様の曖昧さを見つけたら明示する
- 過剰な抽象化を避ける
- 1000行の複雑な解決より、100行の明快な解決を優先する
- 関係ないファイル、コメント、挙動を変更しない
- Dead Codeを残さない
- セキュリティ、権限、入力検証を軽視しない

## Workflow

1. 変更前に関連ファイルを探索する
2. 実装前に短い計画を出す
3. TDDを実行する
4. 失敗したらログを読んで原因を説明する
5. 最後に、変更点・確認済み項目・未確認リスクを報告する
6. SubAgentを使用してレビューを行ってもらい、結果に対してユーザと検討する
7. Step6に対して検討した結果、対応が必要な場合は対応する

## Review Checklist

- この変更は本当に必要か
- もっと単純にできないか
- 既存仕様を壊していないか
- エラー処理は十分か
- テストは意味のある失敗を検出するか
- 型、Lint、Build、主要テストは通るか

## Communication

- 不確かなことを断定しない
- トレードオフを明示する
- ユーザーが間違った方向に進んでいる場合は丁寧に止める
- 実装完了を主張する前に証拠を示す

## Technology used

- プログラミング言語: Rust
- TUIフレームワーク: Ratatui
- 開発環境管理：Nix

## Command

- `nix build`: プロジェクトのビルドを行います
- `nix fmt`: ソースコードのフォーマットを行います
- `nix fmt -- --ci`: フォーマット差分を検証します（ファイルを変更しない。CIと同じチェック）
- `nix run .#lint`: ソースコードのリントを行います
- `nix run .#test`: ソースコードのテストを行います

PRはCIでformat/lint/test/buildが全て通る必要があります。push前にこの4つで確認してください。

## Skill Routing

必要に応じて以下のスキルを使用してください

- `commit` : commitする際に使用する
- `tdd` : tddを実行する際に使用する

## Git Rules

### 主要な規約

- 作業する場合は、必ずブランチを切って作業しなければならない
- コミットは変更の最小単位に分割しなければならない
- PRのメッセージには特に確認してほしい観点を追加すること
- commit message及びPR messageは英語で記載しなければならない

### ブランチ名

- 機能追加の場合:`feat/*`
- バグ修正の場合:`fix/*`

### PR Title

- PRタイトルに絵文字を使用してはいけない
